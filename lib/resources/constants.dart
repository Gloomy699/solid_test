/// The duration of the animation.
const kAnimationDuration = Duration(milliseconds: 300);

/// The maximum value for RGB color components.
const kMaxRGBValue = 256;

/// The maximum value for the slider.
const kMaxSliderValue = 255.0;

/// The minimum value for the slider.
const kMinSliderValue = 1.0;

/// A wish text message.
const kWishText = 'I wish you a good day';

/// A greeting text message.
const kHeyText = 'Hey there';

/// A text message indicating a change.
const kChangeText = 'Change me';

/// A text message indicating opacity.
const kOpacityText = 'opacity';
